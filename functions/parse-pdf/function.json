{
  "description": "Java example function with existing build.gradle",
  "handler": "lambda.ParsePDF::handler",
  "runtime": "java",
  "hooks": {
    "build": "gradle build",
    "clean": "gradle clean"
  }
}
